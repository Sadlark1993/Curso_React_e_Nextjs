/*the lessons will be recorded in the commits. I will not crate several projects, each one with
a different lesson*/

//created files .editorconfig, .prettierrc.js, .eslintrc.js and added the config of the teacher.

npm i -D prettier eslint-config-prettier eslint-plugin-prettier

//adjust the files
npx eslint src/**/*.js --fix

//adjust the .jsx filles
npx eslint src/**/*.js --fix

npm i prop-types

//link to read: https://pt-br.reactjs.org/docs/hooks-state.html
